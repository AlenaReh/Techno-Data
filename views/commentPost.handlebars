<div class="comments">
  {{#each this}}
  <section class="comment-content">
    <div class="meta">
      {{user.name}} on {{format_date createdAt}}
    </div>
    <div class="text">
      {{comment}}
    </div>
  </section>
  {{/each}}
</div>