<div class="card">
  <header class="card-header card-header-title">
    <div class="column">

      <a href="/post/{{id}}" class="is-pulled-left">{{title}} by {{user.name}} on {{format_date date_created}}</a></p>
    </div>

    <div class="column">
    </div>

  </header>

  <div class="card-content">
    <div class="content has-text-justified">
      {{text}}
    </div>
  </div>